cmake_minimum_required(VERSION 2.8)
project( concurent )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( concurent project.cpp affichage.hpp affichage.cpp)
target_link_libraries( concurent ${OpenCV_LIBS} )
